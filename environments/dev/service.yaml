apiVersion: v1
kind: Service
metadata:
  name: hopeful-api
  namespace: gitops-demo
  labels:
    app.kubernetes.io/name: hopeful-api
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: hopeful-api
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
